//should mount as middleware
	//should mount in a vhost
	//should pass a working reverser
	//should render the HTML page, and render intermediate routes correctly

//should expose REST methods.
	//should allow listing 
	//should allow getting a single item
	//should allow saving a single item
	//should allow saving a list (in progress
	//should allow uploading a file

